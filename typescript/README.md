# LoopEngine (TypeScript)

A TypeScript/Bun port of the Python LoopEngine library.

## Development

Install dependencies:

```bash
cd typescript
bun install
```

Type-check:

```bash
bun run build
```

Run tests:

```bash
bun test
```
